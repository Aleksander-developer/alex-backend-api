/*
npm init -> tutto ok
npm i express
npm i @types/express @types/node nodemon ts-node typescript express
npm i mongoose dotenv cors
npm install -g typescript
npx tsc --init
npm i --save-dev @types/cors

npm install mongodb
npm install mongoose

npm install nodemailer
npm install --save-dev @types/nodemailer




npx nodemon src/app.ts -> per fare partire il Nodemon
Avviare il server in sviluppo con npm run dev
Compilare con npm run build
Lanciare la versione compilata con npm start

*/

/*
Struttura Backend
Node.js + Express + MongoDb Atlas

alex-dev-BE/
├── node_modules/
├── dist/                       # (generato dopo build TypeScript)
├── src/
│   ├── config/
│   │   └── db.config.ts        # Connessione a MongoDB Atlas
│   ├── controllers/
│   │   └── newsletter.controller.ts
│   ├── models/
│   │   └── newsletter.model.ts
│   ├── routes/
│   │   └── newsletter.routes.ts
    │    ├── progetti.routes.ts
    │    ├── chi-sono.routes.ts
    │    ├── contatti.routes.ts
    │    ├── servizi.routes.ts
│   ├── utils/
│   │   └── email.ts
│   └── app.ts                  # Entry point principale del backend
├── .env                        # Variabili ambiente (PORT, MONGO_URI)
├── package.json
├── tsconfig.json
├── package-lock.json

*/

/*
username = aleksanderdeveloper
pass = alexdeveloper

La tua password per l'app per il dispositivo Email
 
EMAIL_USER=aleksandernikollibusiness@gmail.com
EMAIL_PASS=iogjdwjoqoxojgck


*/

/*
Nel terminale, avvia l'app con: npm run dev

*/

/*

github dove si trova il frontend
https://github.com/Aleksander-developer/alex-frontend.git

github dove si trova backend
https://github.com/Aleksander-developer/alex-backend-api.git

render prende backend da github
https://alex-backend-api.onrender.com

netlify prende il frontend da github
https://aleksandernikolli.netlify.app/

il database è su mongodb Atlas
mongodb+srv://<db_username>:<db_password>@cluster0.dmmhwoj.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
*/